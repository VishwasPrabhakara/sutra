# Sutra Frontend

React and TypeScript client for Sutra's streaming multi-agent interface.

```powershell
npm ci
npm run lint
npm run build
npm run dev
```

Set `VITE_API_BASE` in `.env` to the FastAPI backend URL.
